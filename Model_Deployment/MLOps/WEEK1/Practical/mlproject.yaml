name: customer-churn

conda_env: conda.yaml

entry_points:
  main:
    parameters:
      learning_rate: {type: float, default: 0.01}
      epochs: {type: int, default: 50}
    command: "python train.py --lr {leanring_rate} --epochs {epochs}"